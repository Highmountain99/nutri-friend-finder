import { ProgressRouter } from "@/components/progress/ProgressRouter";

export default function Progress() {
  return (
    <div className="animate-fade-in relative">
      <ProgressRouter onOpenJourney={() => {}} />
    </div>
  );
}
